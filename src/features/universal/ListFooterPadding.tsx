import React from "react";
import { View } from "react-native";
import { LIST_FOOTER_PADDING } from "../../constants";

const ListFooterPadding = () => {
    return <View style={{ paddingBottom: LIST_FOOTER_PADDING }} />;
};

export default ListFooterPadding;
